extension Dictionary {
    static func + (lhs: Dictionary, rhs: Dictionary) -> Dictionary {
        lhs.merging(rhs) { (_, new) in new }
    }
}

let mergedDict = dict1 + dict2
print(mergedDict) // ["a": "apple", "b": "banana", "c": "cherry", "d": "date"]