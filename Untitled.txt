   var body: some View {
        List {
            ForEach(items, id: \.0) { item in
                CustomCell(title: item.0, subtitle: item.1)
                    .listRowInsets(EdgeInsets()) // Removes default padding
                    .listRowSeparator(.hidden) // Removes default separator
                    .background(Color.clear) // Avoids background color issues
            }
        }
        .listStyle(.plain) // Removes extra padding
        .background(Color.gray.opacity(0.2)) // Background color for better visibility
    }