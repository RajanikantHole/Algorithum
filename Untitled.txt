struct ContentView: View {
    var body: some View {
        VStack {
            RoundedContainer(color: .blue, cornerRadius: 12) {
                Text("Hello, SwiftUI!")
                    .foregroundColor(.white)
            }
            
            RoundedContainer(color: .red, cornerRadius: 20) {
                VStack {
                    Image(systemName: "star.fill")
                    Text("Star")
                }
                .foregroundColor(.white)
            }
        }
        .padding()
    }
}