import SwiftUI

struct ContentView: View {
    var body: some View {
        ScrollView {
            VStack(spacing: 20) {
                Text("Scroll Above and Below PageView")
                    .font(.title)

                TabView {
                    Color.red
                    Color.green
                }
                .tabViewStyle(.page)
                .frame(width: UIScreen.main.bounds.width, height: 300) // Full width
                .clipped()
                
                Text("More content below")
                    .font(.title)
            }
            .frame(maxWidth: .infinity)
        }
    }
}