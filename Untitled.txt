import SwiftUI

struct ContentView: View {
    let epochTime: TimeInterval = 1709505600 // Example epoch time
    
    var body: some View {
        Text(DateUtils.formatEpoch(epochTime, format: "yyyy-MM-dd HH:mm:ss", timeZone: .current))
            .padding()
    }
}